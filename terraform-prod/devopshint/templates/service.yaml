apiVersion: v1
kind: Service
metadata:
  name: {{ include "devopshint.fullname" . }}
  labels:
    {{- include "devopshint.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devopshint.selectorLabels" . | nindent 4 }}
